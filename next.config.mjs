/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["as1.ftcdn.net","img.freepik.com","p4.wallpaperbetter.com"],
      },
    
};

export default nextConfig;
